# 1주차 질문노트

> 팀장님께 물어볼 것만 기록. AI한테 물어볼 수 있는 건 제외.

---

## 업무 방식

### Q1. 업무요청서의 예제 코드를 내 마음대로 바꿔도 되는가?
- 예: `red.py`에서 `subprocess` 자동 설치 제거, `imshow` 대신 `imwrite`로 저장, 함수화
- 업무요청서에 제시된 코드는 "가이드"인가, "정답"인가?
- 개선했다고 생각하는 부분을 자유롭게 반영해도 되는지, 아니면 가이드에 충실히 따라야 하는지

---

## subprocess / pip install

### Q2. 스크립트 안에 `pip install`을 넣는 게 실무에서도 쓰이는가?
- 매 실행마다 `subprocess`로 패키지 설치 체크하는 패턴
- 실무에서는 보통 `requirements.txt` + README로 의존성 관리 vs 스크립트 자동 설치
- 어떤 방식이 더 일반적인지, 장단점이 뭔지
